
// Explica este código JavaScript

function sayHi(name) {
  return `Hi there, ${name}`;
}
console.log(sayHi()); // 🤔?

// Respuesta Correcta: B) Hi there, undefined
// En javascript los parámetros tienen por defecto el valor undefined, 
// }esto quiere decir que sino pasamos ningún parámetro a una función que los necesite tendremos undefined.
